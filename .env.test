PORT=3000
NODE_ENV=test

# Test Database Configuration (PostgreSQL)
DB_USERNAME=lms_user
DB_PASSWORD=lms_password
DB_DATABASE=leave_management_test
DB_PORT=5432
DB_HOST=localhost

# JWT Configuration
JWT_SECRET=password
JWT_EXPIRATION=24h
